class Solution {
    fun isPalindrome(x: Int): Boolean {

        var num = x.toString();
        var start = 0;
        var end = num.length-1;

        while (start<=end){
         if(num[start] != num[end]){
             return false
         }
            start++;
            end--;
        }
        return true;
    }
}
